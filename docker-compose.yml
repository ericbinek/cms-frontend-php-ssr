name: cms-frontend-php-ssr

services:
  app:
    container_name: cms-frontend-php-ssr
    build: .
    ports:
      - "4002:4002"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      PORT: "4002"
      API_BASE_URL: "${API_BASE_URL:-http://host.docker.internal:3002}"
