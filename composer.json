{
    "name": "cms/cms-frontend-php-ssr",
    "description": "Server-rendered schema.org-aligned CMS frontend on PHP — no runtime dependencies.",
    "type": "project",
    "version": "0.2.0",
    "license": "MIT",
    "authors": [
        {
            "name": "Eric Binek"
        }
    ],
    "require": {
        "php": ">=8.5"
    },
    "autoload": {
        "psr-4": {
            "Cms\\": "src/"
        }
    },
    "scripts": {
        "test": "php bin/test.php"
    }
}
